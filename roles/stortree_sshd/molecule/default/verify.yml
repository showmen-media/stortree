---
- name: Verify
  hosts: all
  gather_facts: false
  tasks:
    - name: Stortree_sshd produced the resolved stortree fact
      ansible.builtin.assert:
        that:
          - stortree is defined
          - stortree.server_subtrees is defined
          - stortree.subtree_mounts is defined
