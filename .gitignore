# Real, site-specific config -- never committed. Copy the *.example
# files instead (see README.md "Setup").
inventory/hosts.yml
inventory/group_vars/*.yml
inventory/host_vars/*.yml
stortree/config.yml
stortree/ldap.yml
stortree/rclone.conf
stortree/sshd_config
*.vault-pass
.vault_pass

# Local tooling
.venv/
__pycache__/
*.pyc
.pytest_cache/
.cache/
.coverage
.coverage.*
htmlcov/
collections/

# Generated from the fleet, not edited: names real hosts and addresses.
prometheus/stortree-targets.json

# Molecule / Ansible run artifacts
.molecule/
*.retry


*.log
